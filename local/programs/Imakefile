XCOMM $NetBSD: Imakefile,v 1.5 2002/06/18 09:04:00 tron Exp $

#define IHaveSubdirs
#define PassCDebugFlags CDEBUGFLAGS="$(CDEBUGFLAGS)"

#ifndef UseFreetype2
#define UseFreetype2	NO
#endif

#if UseFreetype2
        FTSRCDIR = xftlsfonts
#endif

#if defined(NetBSDArchitecture) && \
    ((OSMajorVersion > 1) || \
     (OSMajorVersion == 1 && OSMinorVersion > 6) || \
     (OSMajorVersion == 1 && OSMinorVersion == 6 && OSTeenyVersion > 1))
        XSSRCDIR = xsystrace
#endif

SUBDIRS = x11-ssh-askpass $(FTSRCDIR) $(XSSRCDIR)

MakeSubdirs($(SUBDIRS))
DependSubdirs($(SUBDIRS))
